============
Installation
============

At the command line::

    $ easy_install django_authstrap

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django_authstrap
    $ pip install django_authstrap